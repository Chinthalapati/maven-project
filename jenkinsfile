pipeline {

    agent any 

    stages {

        stage('Build') {

            steps {
                sh 'clear'
            }
            post {
                success {
                    echo 'Now Archiving...'
                    archiveArtifacts artifacts: '**/target/*.war'
                }
            }
        }
        stage('Deploy to Stage') {
            steps {
                build job: 'deploy-to-staging'
            }
        }
    
    }

}